#include "local_search.h"
#include <iostream>
#include <vector>
#include <numeric>


bool LocalSearch::relocate(Solution& solucion, const VRPLIBReader& reader) {

    std::cout << "Advertencia: La función relocate() aún no está implementada." << std::endl;
    
    return false; // No se encontraron mejoras
}

bool LocalSearch::two_opt_intra_ruta(Solution& solucion, const VRPLIBReader& reader) {
    
    std::cout << "Advertencia: La función two_opt_intra_ruta() aún no está implementada." << std::endl;
    
    return false; // No se encontraron mejoras
} 